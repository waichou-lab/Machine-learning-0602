## 六、期末報告構想（簡述方向）

### 主題方向
**線性複雜度注意力在語意分割中的效率與精度分析**

### 主要研究問題
- CCNet 提出的 criss-cross attention 能否在降低計算成本（\(O(N^2)\) → \(O(N)\)）的同時，維持與 Non-local 相近的語意分割準確率？
- 與純通道注意力（SENet）相比，其全局上下文建模的優勢是否明顯？

### 預期方法
使用 Cityscapes 資料集，比較 CCNet、Non-local 與 SENet 嵌入相同分割網路（如 DeepLabV3+）後的 mIoU、參數量、推論時間與記憶體佔用。

### 可能成果
提供一份效率與精度權衡的對比表格與圖表，並給出實務部署建議。
